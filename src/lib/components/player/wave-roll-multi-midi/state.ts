/**
 * Placeholder – this file will progressively house the functional state store
 * for the WaveRollMultiMidiPlayer.  For now we simply re-export the existing
 * OO StateManager so external imports stay valid while we migrate.
 */

// import { StateManager } from "./state";
